interface:
  display_name: "GitWarp"
  short_description: "Git worktree isolation and task records for agents"
  default_prompt: "Use GitWarp before parallel agent work to start an isolated workspace, read the dossier, and record handoffs."
